/**
 * Extension-wide constants and default thresholds.
 */

export const DEFAULT_KEEP_RECENT = 20;
export const MAX_SESSION_TITLE_LEN = 72;

export const DEFAULT_QUOTA_INFO_RATIO = 0.7;
export const DEFAULT_QUOTA_WARN_RATIO = 0.9;

export const STATUS_KEY = "session-guard";
export const QUOTA_CACHE_TTL_MS = 15_000;
export const WARN_NOTIFY_COOLDOWN_MS = 60_000;
